# نسخ كود تطبيق الترند مباشرة

افتح لوحة **Code** في نسخة المشروع، ثم انسخ محتوى الملفات التالية إلى مشروعك مع الحفاظ على نفس المسارات. لا تحتاج إلى تنزيل ملف مضغوط.

## ملفات الإعداد والتشغيل

| المسار | الغرض |
|---|---|
| `package.json` | الحزم وأوامر التشغيل والاختبارات |
| `client/index.html` | رأس الصفحة وإعدادات SEO والخطوط |
| `client/src/index.css` | الألوان والتصميم العام وRTL |
| `drizzle/schema.ts` | مخطط جداول قاعدة البيانات |
| `drizzle/*.sql` | جميع هجرات قاعدة البيانات |
| `server/db.ts` | استعلامات قاعدة البيانات ومنطق النقاط |
| `server/routers.ts` | واجهات tRPC وعمليات الخادم |

## ملفات الواجهة

| المسار | الغرض |
|---|---|
| `client/src/App.tsx` | المسارات الرئيسية للتطبيق |
| `client/src/components/` | شريط التنقل وبطاقات الفيديو والمنشور والقصص والمشاركة |
| `client/src/pages/` | جميع صفحات الفيديو والقصص والمنشورات والترند والتحديات والإحصاءات |
| `client/src/hooks/` | Hooks للإشعارات وSEO |

## التشغيل بعد النسخ

```bash
pnpm install
pnpm drizzle-kit generate
pnpm dev
```

قبل النشر، اضبط متغيرات البيئة عبر إعدادات المشروع. لا تنسخ ملفات `.env` أو المفاتيح السرية إلى مستودع عام.
